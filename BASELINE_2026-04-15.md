# Baseline - 2026-04-15

## Build & Lint
- frontend lint: PASS
- backend build: PASS
- root health-check: PASS

## Route Smoke
- frontend smoke-routes: PASS
- admin route inventory: PASS
- generated docs:
  - `docs/operations/admin-route-inventory.md`
  - `docs/operations/admin-route-inventory.json`

## Admin E2E (latest known)
- suite: `admin-commercial/config/detail/routes/system`
- result: PASS (39/39)

## Runtime / Ops Checks
- `ops:post-deploy`:
  - status: PASS (with warnings)
  - notes:
    - optional health probes (`/api/health/live|ready|detail`) may return 404 in current live runtime
    - frontend content audit is now opt-in (`OPS_STRICT_CONTENT_AUDIT=1`)
    - observability probe warning remains when `/api/meta/observability` is not exposed
- `ops:security-baseline`:
  - status: PASS (with warning)
  - warning:
    - observability endpoint 404 is treated as warning unless `SEC_REQUIRE_OBSERVABILITY_ENDPOINT=1`
- `ops:oncall-watchdog`:
  - status: PASS (severity=OK, with warning)
  - warning:
    - observability endpoint unavailable is warning unless `WATCHDOG_REQUIRE_OBSERVABILITY=1`
- `ops:admin-ui-live`:
  - latest status: PASS (with warning fallback mode)
  - note:
    - script now supports both login modes:
      - `OPS_ADMIN_EMAIL + OPS_ADMIN_PASSWORD` (preferred)
      - `OPS_ADMIN_KEY/ADMIN_KEY` (legacy)
    - when login is unavailable and `OPS_ADMIN_UI_REQUIRED` is not set, script emits warning report instead of hard-failing

## Backend Quality (Day 3)
- `backend lint`: PASS
- `backend typecheck`: PASS
- `backend test`: PASS (47 suites, 270 tests)
- `backend test:e2e`: PASS (1 suite, 5 tests)
- `backend test:integration`: PARTIAL PASS
  - passed: `test/runtime.integration.spec.ts`
  - skipped: `test/dependencies.integration.spec.ts` (requires `INTEGRATION_TESTS=1` + external DB/Redis)

## Current Commit
- branch: `main`
- commit: `1b1a55a`

## Current Known Risks
1. Advanced health routes (`/api/health/live|ready|detail`) are not consistently exposed in live runtime; currently treated as optional in ops checks.
2. Observability endpoint (`/api/meta/observability`) is not exposed in current live runtime; watchdog/security checks now warn by default.
3. Strict frontend copy contract audit is disabled by default and must be explicitly enabled (`OPS_STRICT_CONTENT_AUDIT=1`) when needed.
4. External dependency integration test suite remains conditional (`INTEGRATION_TESTS=1` + DB/Redis).
5. `ops:admin-ui-live` requires email/password credentials to execute full live interaction checks against the current auth contract.
