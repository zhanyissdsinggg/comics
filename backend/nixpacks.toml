# Railway Nixpacks 配置
# 老王说：确保执行 npm run build 生成 dist 目录
# 强制重新构建 - 2026-03-02

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "node dist/src/main.js"
